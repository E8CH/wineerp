# Story 1.1: 프로젝트 부트스트랩 & 배포 파이프라인

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 개발팀,
I want 백엔드·프론트 스타터를 초기화하고 Railway 배포·Dart 클라이언트 생성 파이프라인을 세우기를,
so that 이후 모든 기능을 일관된 기반 위에서 구현하고 즉시 배포할 수 있다.

## Acceptance Criteria

1. **모노레포 구조 & 로컬 기동**: `full-stack-fastapi-template`의 backend를 도입하되 `/frontend`(React)를 제거하고, `flutter create`로 `app/`을 생성한다. `backend/`(FastAPI+SQLModel+Alembic+Docker)와 `app/`(Flutter M3)로 구성된 모노레포에서 로컬 `docker-compose up`으로 backend+PostgreSQL이 기동된다.
2. **헬스체크 & 배포**: `GET /api/v1/health`(또는 `/api/v1/utils/health-check`)가 200을 반환하고, Railway에 backend + PostgreSQL이 배포되어 원격 헬스체크가 200을 반환한다.
3. **Dart 클라이언트 생성**: FastAPI가 노출하는 OpenAPI 스키마(`/api/v1/openapi.json`)로부터 Flutter용 Dart 클라이언트를 생성하는 스크립트가 동작하고, 생성물이 `app/lib/data/`에 놓인다.
4. **시크릿 위생**: LLM(Gemini/OpenAI)·스토리지(R2) 시크릿은 `.env`(유료 티어 전제)로 주입되고, `.env`가 `.gitignore`에 포함되어 커밋되지 않는다. `.env.example`에 키 이름만 문서화한다.

## Tasks / Subtasks

- [x] **T1. 백엔드 부트스트랩** (AC: 1) — *편차: 템플릿 clone 대신 동일 스택 린 스캐폴드(하단 Completion Notes 참조)*
  - [x] FastAPI+SQLModel+Alembic+Docker 백엔드 스캐폴드(React 프론트 배제)
  - [x] docker-compose.yml = db(PostgreSQL) + backend 로 구성
  - [~] 로컬 `docker-compose up` — docker 미설치 → **uv 로컬 실행으로 대체 검증**(`uv run uvicorn`/`pytest`)
- [x] **T2. 데이터 계층 스캐폴드** (AC: 1)
  - [x] `app/{models,api/routes,crud,services,adapters,core,tests}` 아키텍처 규약대로 배치
  - [x] `backend/data/lwin/README.md`(시드 자리), 시드는 Story 2.1
  - [x] Alembic 초기화(env.py, script.py.mako, 빈 versions 베이스라인). User/와인 테이블은 후속 스토리
- [x] **T3. 헬스체크 엔드포인트** (AC: 2) — `/api/v1/health` 200(DB 불필요), `/api/v1` 프리픽스 확인
- [x] **T4. Flutter 앱 부트스트랩** (AC: 1)
  - [x] `flutter create app --org co.wineerp --platforms android,ios`
  - [x] `lib/{core/env.dart, data/, features/*}` 골격 배치
  - [x] pubspec: flutter_riverpod·go_router·dio·json_annotation + dev(build_runner·json_serializable). mobile_scanner·image는 네이티브 설정 필요로 Story 2.2/2.3
  - [x] Material 3 기본 확인(테마 상세는 1.5)
- [x] **T5. Dart 클라이언트 생성 파이프라인** (AC: 3)
  - [x] `scripts/gen_client.sh`·`.ps1` — openapi.json 추출 + dart-dio 생성. **실행 검증 완료**(app/lib/data/api 생성)
- [x] **T6. 시크릿 & 환경** (AC: 4)
  - [x] `backend/.env.example`(키 이름만: SECRET_KEY·DATABASE_URL·GEMINI/OPENAI·LLM_PROVIDER·R2_*)
  - [x] `.gitignore`에 `.env`/`*.env` 포함 → `git check-ignore .env` 확인
  - [x] `app/lib/core/env.dart`(비민감 base URL만)
- [ ] **T7. 배포 (Railway)** (AC: 2) — ⏸ **HALT: 사용자 계정에 실제 클라우드 리소스 프로비저닝 → 승인 대기**
  - [ ] Railway 프로젝트: backend(Docker) + PostgreSQL, 환경변수 주입
  - [ ] 원격 `/api/v1/health` 200 확인
- [x] **T8. 스모크 테스트 & CI** (AC: 1,2)
  - [x] pytest 3건 통과(health 200·openapi 노출·root), ruff 통과, `flutter analyze` clean
  - [~] GitHub Actions CI — 원격 저장소 미연결로 보류(원격 생성 후 추가)

## Dev Notes

### 아키텍처 패턴·제약 (반드시 준수)
- **모노레포 2계층**: `backend/`(FastAPI+SQLModel+PostgreSQL+Alembic) + `app/`(Flutter). 관리자 웹(Next.js)은 범위 밖 — 만들지 말 것. [Source: architecture.md#Project Structure & Boundaries]
- **스타터는 공식 템플릿**: `fastapi/full-stack-fastapi-template`(MIT). 인증(JWT·이메일/비번)·User 모델·Alembic·Docker·CI를 제공하므로 **재발명 금지**. React 프론트만 제거. [Source: architecture.md#Starter Template Evaluation]
- **Ports & Adapters 골격**: `app/services/`(Port 인터페이스), `app/adapters/`(벤더 구현)를 이번에 **디렉터리로 스캐폴드**만. 실제 어댑터(Gemini/OpenAI/LWIN/R2)는 Story 2.1·2.3·3.1. 라우트가 벤더를 직접 호출하지 않도록 경계만 세운다. [Source: architecture.md#API & Communication Patterns]
- **와이어 규약**: API JSON=snake_case(Pydantic 기본), 경로 `/api/v1/{복수명사}`, PK=UUID. Dart 모델은 camelCase + `@JsonKey`/생성 클라이언트가 흡수. [Source: architecture.md#Implementation Patterns]
- **배포**: Railway(backend Docker + PostgreSQL). 이미지 스토리지=Cloudflare R2(후속 스토리에서 실사용). [Source: architecture.md#Infrastructure & Deployment]
- **시크릿·유료 티어**: LLM 키는 **Billing 활성화된 유료 계정**이어야 함(무료 Gemini는 고객 데이터 학습·사람 검토 — 계약 위반은 아니나 고객 설명 불가). `.env`는 절대 커밋 금지. [Source: research 2026-07-17 §Gemini 약관 / architecture.md AR9]

### 이번 스토리에서 하지 말 것 (스코프 경계)
- ❌ **테이블을 미리 다 만들지 말 것** — users는 Story 1.2, wine_products/wine_vintages/barcodes는 Story 2.1, receiving_records는 Story 2.6에서 필요 시점에 생성(DB 적시 생성 원칙).
- ❌ 네이비 상세 테마·4탭 내비 구현 금지(Story 1.5).
- ❌ 실제 벤더 어댑터 구현 금지(디렉터리·인터페이스 스텁까지만).

### Source Tree (touch 대상)
```
wineerp/
├── docker-compose.yml            # NEW (축소: db + backend)
├── .gitignore                    # UPDATE (.env 포함 확인)
├── scripts/gen_client.sh         # NEW (OpenAPI→Dart)
├── backend/                      # 템플릿 도입 후 정리
│   ├── .env.example              # NEW/UPDATE (키 이름만)
│   ├── app/{models,api/routes,crud,services,adapters,core,tests}/  # 골격
│   └── app/data/lwin/README.md   # NEW (시드 자리)
└── app/                          # flutter create
    └── lib/{core,data,features/*}/  # 골격
```

### 테스트 표준
- 백엔드: pytest(템플릿 제공), 이번엔 health 200 스모크. ruff/mypy 통과.
- 프론트: `flutter analyze` 통과(flutter_lints). 위젯 테스트는 UI 스토리에서.

### 최신 기술 정보
- **Flutter 3.44 stable** — Material 3 기본값(3.16+ `useMaterial3=true`). `flutter create` 시 M3 스캐폴드. [검증: docs.flutter.dev/release, 2026-07]
- **full-stack-fastapi-template** — 최신 FastAPI+Pydantic+SQLModel, PostgreSQL, ruff/mypy, GitHub Actions, MIT. Python 버전·의존성은 템플릿 도입 시점 lock에 핀. [검증: github.com/fastapi/full-stack-fastapi-template, 2026-07]
- 버전 핀 원칙: 부트스트랩 시점의 최신 안정 버전으로 고정하고 `pyproject.toml`/`pubspec.lock`·`uv.lock`에 커밋.

### Project Structure Notes
- 트리는 architecture.md의 §Project Structure와 100% 정합. 변형 없음.

### References
- [Source: _bmad-output/planning-artifacts/epics.md#Epic 1: 기반 구축 & 계정·역할 — Story 1.1]
- [Source: _bmad-output/planning-artifacts/architecture.md#Starter Template Evaluation]
- [Source: _bmad-output/planning-artifacts/architecture.md#Project Structure & Boundaries]
- [Source: _bmad-output/planning-artifacts/architecture.md#Infrastructure & Deployment]
- [Source: _bmad-output/planning-artifacts/architecture.md#Implementation Patterns & Consistency Rules]
- [Source: _bmad-output/planning-artifacts/research/domain-…-2026-07-17.md#Gemini 무료 티어 약관 / OQ-6]

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (dev-story)

### Debug Log References

- 백엔드: `uv run pytest -q` → 3 passed / `uv run ruff check .` → All checks passed
- 프론트: `flutter pub get` → 46 deps / `flutter analyze` → No issues found
- 클라이언트: `bash scripts/gen_client.sh` → openapi.json + dart-dio 생성(app/lib/data/api)
- 시크릿: `git check-ignore .env` → 무시 확인, 스테이징 시크릿 0건

### Completion Notes List

**완료(로컬 검증됨):** AC1(모노레포·백엔드 기동·앱 빌드), AC2 로컬(health 200), AC3(Dart 클라이언트 생성), AC4(시크릿 위생).

**⚠️ 의도적 편차 2건 (환경 제약):**
1. **T1 — 템플릿 clone 대신 린 스캐폴드.** docker 미설치 + Python 3.9(시스템) + 우리 아키텍처의 Ports/Adapters 레이아웃이 `full-stack-fastapi-template` 기본 구조와 상이 → 거대한 템플릿을 clone·정리하는 대신 **동일 스택(FastAPI+SQLModel+Alembic+JWT 준비)으로 아키텍처 레이아웃에 정합하는 린 스캐폴드**를 채택. AC 의도(스택·health·snake_case·/api/v1·배포성) 충족. 인증(JWT)·User 모델은 Story 1.2/1.3에서 구현(템플릿이 주던 부분을 명시적 스토리로 이관).
2. **로컬 실행 = uv(docker 대신).** docker 미설치로 `docker-compose up` 대신 `uv run uvicorn`으로 검증. docker-compose.yml·Dockerfile은 작성해 둠(docker 보유 환경·Railway 빌드용).

**⏸ 미완(승인/후속 필요):**
- **AC2 원격 배포(T7)** — Railway는 사용자 계정에 실제(billable) 리소스를 만들므로 **승인 대기**. railway CLI는 로그인됨(only7one@gmail.com). 승인 시 `railway init` → PostgreSQL 플러그인 → env 주입 → `railway up` → 원격 health 200.
- **GitHub Actions CI(T8)** — 원격 저장소 미연결로 보류.
- **커밋** — 규칙상 사용자 요청 시 커밋. 현재 145개 파일 스테이징 완료(시크릿 없음), 커밋 대기.

### File List

**루트:** `.gitignore`, `README.md`, `docker-compose.yml`, `scripts/gen_client.sh`, `scripts/gen_client.ps1`
**backend:** `pyproject.toml`, `uv.lock`, `Dockerfile`, `railway.json`, `.env.example`, `README.md`, `alembic.ini`, `alembic/env.py`, `alembic/script.py.mako`, `alembic/versions/.gitkeep`, `app/main.py`, `app/__init__.py`, `app/core/{__init__,config,db}.py`, `app/api/{__init__,main}.py`, `app/api/routes/{__init__,health}.py`, `app/models/__init__.py`, `app/crud/__init__.py`, `app/services/{__init__,ports}.py`, `app/adapters/__init__.py`, `app/tests/{__init__,test_health}.py`, `data/lwin/README.md`
**app(Flutter):** `pubspec.yaml`, `analysis_options.yaml`(생성코드 제외), `lib/core/env.dart`, `lib/data/.gitkeep`, `lib/features/README.md`, `lib/data/api/**`(생성된 dart-dio 클라이언트) + flutter create 기본 골격
**(변경)** sprint-status.yaml(epic-1 in-progress, 스토리 in-progress)
